#include<stdio.h>

#define equation(a,b,c,x) (((a) * (x) + b) * x + c)

int main()
{
	float a,b,c;
	scanf("%f%f%f",&a,&b,&c);

	int roughRoot = -100;
	for (int answerCount = 0;answerCount < 3;answerCount++) {
		
	}
}
